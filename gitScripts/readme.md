По идее потом сделаем скрипт, где будем запускать команды
каждая команда делает коммит (по крайней мере пока в идее это, как работать с конфликтами пока не знаю)
тогда new-branch например будет содержать гит коммит
а update-files нет
потому что это вспомогательная, работающая только внутри остальных
будем юзать
new-commit
new-branch
merge-branch
и подобное, я пока не все в голове представляю, что надо

мб потом переделаем хранение скриптов, чтобы явно было понятно, что используем, а что "системное"
ну или если есть другая идея сделать хорошо, и чтобы читабельность итогового скрипта была хорошая, тоже предлагай


# То, что уже реализовано
- init - создание начального коммита
- new-branch - создание новой ветки TODO: переписать на смену ветки, с возможностью создать
- set-user - сменить пользователя
- commit - создание простого коммита

# Вспомогательные
- update-files - заменить папку на новую версию

# Надо реализовать
- мердж веток